#!/bin/sh

app_id=$1

payload="{ id: '$app_id', name: '$app_id', dir: '$PWD' }"
curl -X POST \
  --header "Content-Type:application/json" \
  http://10.0.1.140:8080/admin/applications \
  --data "$payload"

